<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Abril+Fatface&family=Poppins:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <nav class="fixed inset-x-0 top-0 z-10 w-full px-4 py-1 bg-gray-50 shadow-md border-gray-500 transition duration-700 ease-out">
            <div class="flex justify-between md:p-4">
                <a href="{{ path('index') }}" class="md:text-[2rem] text-base leading-[3rem] tracking-tight font-bold text-gray-900">NewPractice</a>
                <div class="flex items-center md:space-x-4 space-x-2 md:text-lg text-sm font-semibold tracking-tight">
                    <a href="{{ path('index') }}" class="px-6 py-2 text-gray-900 transition duration-700 ease-out bg-gray-50 border border-gray-900 rounded-lg ">Acceuil</a>
                    <a href="{{ path('task_create') }}" class="px-6 py-2 text-gray-50 transition duration-500 ease-out bg-gray-900 rounded-lg hover:bg-gray-800 hover:ease-in">Ajouter</a>
                </div>
            </div>
        </nav>
        <div class="mt-20">
            {% block body %}{% endblock %}
        </div>
    </body>
</html>
